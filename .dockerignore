# Keep the Docker build context small. The Dockerfile rebuilds web/dist and
# node_modules inside the image, so they (and other local build output) are
# excluded here.
.git/
target/
dist/
**/node_modules/
web/dist/
.claude/
